import { FC } from "react";
import { IPages } from "../interfaces/page";

const Edit: FC<IPages> = (props) => {
    return (
        <div>
            <h1>Edit</h1>
        </div>
    );
};

export default Edit;